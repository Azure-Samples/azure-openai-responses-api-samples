# Environment variables for Azure OpenAI Responses API samples
#
# Aligned with Microsoft Agent Framework (see:
# https://github.com/microsoft/agent-framework/blob/main/python/.env.example )
#
# This repo also supports legacy variable names for backwards compatibility.

# Azure OpenAI (preferred)
AZURE_OPENAI_ENDPOINT=""                       # e.g. https://YOUR-RESOURCE-NAME.openai.azure.com/
AZURE_OPENAI_RESPONSES_DEPLOYMENT_NAME=""      # deployment name for Responses API samples
AZURE_OPENAI_CHAT_DEPLOYMENT_NAME=""           # optional fallback (some tooling uses this name)

# Auth (API key samples)
# Agent Framework uses OPENAI_API_KEY for OpenAI. For Azure OpenAI key-auth samples,
# we accept either OPENAI_API_KEY or AZURE_OPENAI_API_KEY.
OPENAI_API_KEY=""

# Legacy (non-v1) AzureOpenAI client only (v1 samples don't need api-version)
AZURE_OPENAI_API_VERSION="2024-10-21"

# ----- Backwards compatible aliases (older versions of these samples) -----
# AZURE_OPENAI_API_ENDPOINT=""                 # old name for AZURE_OPENAI_ENDPOINT
# AZURE_OPENAI_API_MODEL=""                    # old name for deployment name
# AZURE_OPENAI_API_KEY=""                      # old name for key-auth
# AZURE_OPENAI_V1_API_ENDPOINT=""              # optional override; otherwise derived from AZURE_OPENAI_ENDPOINT
